# 更新日志

本文件用于记录项目每个版本的主要变化。

## V1.0.0

### 重构

- 将 `POST /api/check`、`GET /api/check-progress/{progressId}` 和 `GET /api/jobs/{jobId}` 请求从 `App.vue` 抽离到 `src/api/documentCheckers.js`。
- `App.vue` 继续负责页面状态、进度轮询、结果应用和本地缓存，保持现有后端接口不变。
- 新增前端现状分析与分阶段重构文档。

### 新增

- 上传区域逐项展示已选择的 Word 文件，长文件列表支持滚动查看。
- 新增英文格式检查和 Figure 引用检查自动化测试。
- Web 结果持久化增加进程内互斥锁，串行处理同名文档归档和重复任务替换。

### 优化

- 识别 Word 中的代码、命令和控制台段落样式，避免对独立命令执行普通英文格式检查。
- 英文格式检查保护 URL、IP 地址、文件路径、域名、版本号、文件名、哈希和命令参数等特殊内容。
- Figure 检查分别识别图题和正文引用，使问题类型与提示信息更加明确。
- 上传面板与结果预览面板保持一致高度，并改善窄屏布局。

### 修复

- 修复并发请求同时更新同名文档或同一文件组合时可能发生的 Windows 目录替换冲突。
- 修复 Word 自动编号 XML 中的 `Figure 61` 无法与正文 `Figure 6-1` 匹配的问题。
- 保证 `Figure 1-23` 和 `Figure 12-3` 等相似编号不会错误匹配。

## V0.5.0

### 新增

- 新增 FastAPI Web API，支持一次上传并检查一个或多个 `.docx` 文档。
- 新增 Vue Web 页面，支持 Sheet 结果预览以及 Excel、Markdown 下载。
- 新增检查进度接口和前端进度条，展示当前文件与处理阶段。
- 新增历史任务恢复、同文件组合结果替换和单文档独立归档。
- 新增 `start.ps1` 一键启动脚本。
- 新增 Web 系统分析文档，说明上传协议、存储生命周期、接口结构和当前不足。

### 优化

- 文档检查通过工作线程运行，使检查期间的进度查询仍可响应。
- 重复上传采用临时目录和备份目录替换，避免失败结果覆盖已有成功任务。

## V0.4.1

### 新增

- 支持按输入分组单独执行检查，例如 `python main.py SuperPOD`。
- 支持传入输入分组路径执行检查，例如 `python main.py .\input\SuperPOD\`。

### 修复

- 修复附录类字母标题被挂到前一数字章节下的问题。
- 修复 `A. Exit Status Codes`、`B. JSON Output Format` 等字母附录标题在标题目录中编号为空的问题。
- 修复部分 Word 自动编号 Figure 题注被读取为 `Figure - 标题` 或 `Figure 11 标题` 后无法与正文 `Figure 1-1 标题` 匹配的问题。

## V0.4.0

### 新增

- 新增一级标题分页检查。
- 新增一级标题前多余空段落检查。
- 新增 Figure 标题引用检查，提示可能未被正文引用的 Figure 编号。
- 新增同一最小小节内重复正文段落检查。

### 优化

- 报告文件改为固定文件名，每次运行覆盖刷新。
- 优化相对四级标题的问题定位显示，报告中会带上最近父级标题。

## V0.3.0

### 重构

- 将原有单脚本结构重构为模块化目录结构。
- 新增 `main.py` 作为统一运行入口。
- 新增 `parser/` 目录，用于 Word 文档读取和标题目录解析。
- 新增 `checker/` 目录，用于集中管理各类检查逻辑。
- 新增 `config/` 目录，用于集中维护公共配置、正则规则和厂商关键词。
- 新增 `report/` 目录，用于集中处理 txt 和 Excel 报告导出。
- 更新 README 中的目录说明和运行方式，统一使用 `python main.py`。

## V0.2.0

### 新增

- 新增 `check_heading_catalog.py`，用于检查标题目录中的标题编号。
- 标题检查结果增加 `完整标题` 列，方便根据报告定位具体标题。

### 修复

- 修复 Heading 4 中 `1.`、`2.`、`3.` 这类相对编号被误判为重复的问题。
- Heading 4 相对编号现在按最近的上级标题范围检查连续性，不再按全文编号重复判断。
- 支持 Heading 4 相对编号直接挂在 Heading 2 或 Heading 1 下的情况。
- 保留 `1.2.3.4` 这类完整多级编号的原有重复和连续性检查逻辑。

## V0.1.0

### 新增

#### Word 文档读取

- 支持读取 `.docx` 格式文档。
- 支持读取正文、表格、页眉、页脚。
- 支持批量处理多个 Word 文档。

#### 中文残留检查

- 检查英文文档中的中文汉字残留。
- 定位中文残留所在章节和位置。

#### 厂商信息检查

- 检查厂商名称、官网、邮箱、电话、地址等关键字残留。

#### 英文格式检查

- 检查中文标点。
- 检查连续空格。
- 检查常见英文格式问题。

#### 检查报告生成

- 生成文档读取结果。
- 生成综合检查 Excel 报告。
- 支持多文档汇总统计。
- 为每个文档生成独立检查结果。
